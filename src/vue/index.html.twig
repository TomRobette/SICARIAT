{% extends "base.html.twig" %}

{% block title %}{{parent()}}Accueil{% endblock %}

{% block contenu %}
<!-- TOUT ICI SERA À ADAPTER
<div class="container-fluid">           
    <div class="row justify-content-center">            
        <h1 class="text-center text-primary p-4">Les nouveaux utilisateurs</h1>        
    </div>        
    
    <div class="row justify-content-center">            
        <div class="col-8 bg-white p-4 m-0 text-primary">                
            <ul class="list-group">
                {% for user in users %}
                <li class="list-group-item list-group-item-{{cycle(['secondary','light'], loop.index0)}}">
                    <div class="card xs-4" style="max-width: 300px; max-height: 45px;">
                        <div class="row no-gutters">
                        {% if user.image is defined %}
                          <div class="col-3 float-md-left">
                            <img src="images/{{user.image}}" class="card-img" alt="..." style="max-height: 45px;">
                          </div>
                        {% endif %}  
                          <div class="col-xs-9">
                            <div class="card-body-responsive">
                              <a class="card-title">{{user.login}}</a>
                            </div>
                          </div>
                        </div>
                      </div>
                </li>
                {% endfor %}
            </ul>        
        </div>        
    </div>
</div>


{% set nb = form.nbpages %}
    <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center m-4 p-4">
            <li class="page-item {% if form.nopage == 0 %}disabled{% endif %}">
                <a class="page-link" href="index.php?page=accueil&id={{form['article'].idArticle}}&nopage={{ 0 }}" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>
            {% for i in 1..nb %}
                <li class="page-item {% if i ==form.nopage +1 %} active{% endif %}"><a class="page-link" href="index.php?page=article&id={{form['article'].idArticle}}&nopage={{ i-1 }}">{{ i }}<span class="sronly"></span></a></li>
            {% endfor %}
            <li class="page-item {% if form.nopage == form.nbpages-1 %}disabled{% endif %}">
                <a class="page-link" href="index.php?page=article&id={{form['article'].idArticle}}&nopage={{ nb-1 }}" aria-label="Next" >
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
        </ul>
    </nav>
-->                    
{% endblock %}
