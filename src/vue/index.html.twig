{% extends "base.html.twig" %}

{% block title %}{{parent()}}Accueil{% endblock %}

{% block contenu %}
<!--
<div class="container-fluid">           
    <div class="row justify-content-center">            
        <h1 class="text-center text-primary p-4">Liste des utilisateurs</h1>        
    </div>        
    
    <div class="row justify-content-center">            
        <div class="col-8 bg-white p-4 m-0 text-primary">                
            <ul class="list-group">
                {% for user in users %}
                <li class="list-group-item list-group-item-{{cycle(['secondary','light'], loop.index0)}}">{{user.prenom|capitalize}} {{user.nom|upper}} Date d'inscription : {{user.dateinscription|date('Y-m-d')}} Date de naissance : {{user.datenaissance|date('Y-m-d')}}</li>
                {% endfor %}
            </ul>        
        </div>        
    </div>
</div>




    <li>
                      <div class="card xs-4" style="max-width: 300px; max-height: 45px;">
                        <div class="row no-gutters">
                        {% if session.image is defined %}
                          <div class="col-3 float-md-left">
                            <img src="images/{{session.image}}" class="card-img" alt="..." style="max-height: 45px;">
                          </div>
                        {% endif %}  
                          <div class="col-xs-9">
                            <div class="card-body-responsive">
                              <a href="index.php?page=profil&profil={{session.login}}" class="card-link">{{session.login}}</a>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
-->                    
{% endblock %}
